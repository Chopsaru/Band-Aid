<html>

<link rel="stylesheet" href = "/css/index.css">
<title>Sign up for Band Aid</title>

<div class="bg">
    <br /><br />
    <div class="container forms.container">
        <div class="col-md-6 mx-auto shadow-border form-bg">
            <h1>Update Band Aid Account</h1>
                <form id="edit_user_profile" action="/user_profile" method="post">
                    <div class="form-row">
                        <div class="form-group col-md-6">
                            <label>First Name:</label>
                                <input type="text" class="form-control" name="fname" value="{{user_profile.fname}}">
                        </div>
                        <div class="form-group col-md-6">
                            <label>Last Name:</label>
                                <input type="text" class="form-control" name="lname" value="{{user_profile.lname}}">
                        </div>
                        </div>
                            <label>Email:</label>
                                <input type="text" class="form-control" name="email" value="{{user_profile.email}}">
                            <label>Phone Number:</label>
                                <input type="number" class="form-control" name="phone" value="{{user_profile.phone}}">
                            <label>Zip Code:</label>
                                <input type="number" class="form-control" name="zip" value="{{user_profile.zip}}">
                        <div class="form-row">
                        <div class="form-group col-md-6 ">
                            <label>Instrument</label>
                                <select name="insName" class="form-control">
                                    {{#each instruments}}
                                        <option value="{{id}}">{{insName}}</option>
                                    {{/each}}
                                </select>
                        </div>
                    <div class="form-group col-md-6 ">
                        <label>Skill Level</label>
                        <select id="inputSkill" class="form-control">
                            {{#each proficiencies}}
                                <option value="{{id}}">{{insProficiency}}</option>
                            {{/each}}
                        </select>
                    </div>
                </div>
                <label>Social Media:</label>
                <input type="text" class="form-control" name="social" value="{{user_profile.social}}">
                <label>Demo Link:</label>
                <input type="text" class="form-control" name="demo_link" value="{{user_profile.demo_link}}">
                <div class="checkbox">
                    <label>
                        <input type="checkbox"> Are you looking to join a band?
                    </label>
                </div>
            </form>
            <button onclick="updateUserProfile({{user_profile.id}})" class="btn btn-default send-button">Update Account</button>
            </br></br>
            Click <a href="/user_profile/edit/password/{{user_profile.id}}"">here</a> to change account password
            </br>
            If you want to delete Band Aid User Account click
            <button onclick="areYouSure(); deleteUserProfile({{user_profile.id}})">Delete</button>
        </div>
    </div>
</div>


</html>